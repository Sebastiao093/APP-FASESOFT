import 'package:flutter/material.dart';

class PantallaVotaciones extends StatelessWidget {
  static const routedname = "/PantallaVotaciones";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pantalla Votaciones'),),
    body: Text('pantalla Votaciones'),);
  }
}